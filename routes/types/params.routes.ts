enum QueryParam {
    SEARCH = "search",
    PAGE = "page",
    LIMIT = "limit",
    SORT = "sort",
    ORDER = "order",
    STATUS = "status",
    TYPE = "type",
    VISIBILITY = "visibility"
}

export default QueryParam;