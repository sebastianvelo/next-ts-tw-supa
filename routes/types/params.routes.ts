enum QueryParam {
    ROLE = "role",
    SEARCH = "search",
    PAGE = "page",
    LIMIT = "limit",
    SORT = "sort",
    ORDER = "order",
    STATUS = "status",
    TYPE = "type",
    VISIBILITY = "visibility",
    NAME = "name"
}

export default QueryParam;