import SEGMENTS from "@/routes/types/segments.routes";
import pathOf from "@/routes/utils/pathOf";

const VIEW_ROOT = pathOf(SEGMENTS.USERS);

const VIEW_USER = {
    ROOT: VIEW_ROOT,
};

export default VIEW_USER;
